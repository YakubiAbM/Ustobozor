# backendGreen

Backend: FastAPI, ???????, ??????, ??????, ???????, ?????.

## PostgreSQL (Windows ? ??.)

1. ?????????? PostgreSQL: https://www.postgresql.org/download/windows/
2. ? `.env` ??????? (?????/?????? ??? ??? ?????????):
   ```
   DATABASE_URL=postgresql://postgres:postgres@localhost:5432/market
   ```
3. ???????? ???? **??? psql** ? ?? ???????:
   ```
   pip install -r requirements.txt
   python create_db.py
   ```
4. ???????? ? ??????:
   ```
   python migrate_auth.py
   uvicorn main:app --reload
   ```

???? PostgreSQL ?? ?????????? ??? ?? ??????? ? ??????????????? ?????? `DATABASE_URL` ? `.env`, ????? ????? ?????????????? SQLite (???? `market.db`).
